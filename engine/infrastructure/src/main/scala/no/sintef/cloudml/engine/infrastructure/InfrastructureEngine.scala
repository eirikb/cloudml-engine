package no.sintef.cloudml.engine.infrastructure

import no.sintef.cloudml.repository._

object InfrastructureEngine {

    def apply(instances: List[Instance]) {
        println("So far, so good...")
    }
}
